{
  "name": "protocol",
  "license": "UNLICENSED",
  "scripts": {
    "cb:base-sepolia": "yarn prepare:base-sepolia && yarn codegen && yarn build",
    "cb:base": "yarn prepare:base && yarn codegen && yarn build",
    "prepare:base-sepolia": "mustache ./config/base-sepolia.json subgraph.template.yaml > subgraph.yaml ",
    "prepare:base": "mustache ./config/base.json subgraph.template.yaml > subgraph.yaml ",
    "codegen": "graph codegen",
    "build": "graph build",
    "test": "graph test -d",
    "cbt": "graph codegen && graph build && graph test -d",
    "clean": "rm -rf generated/ build/",
    "cbd:public": "yarn cb:base && graph deploy --studio moxie_protocol_minimal_mainnet -l v1.1.4",
    "cbd:protocol_transactional_testnet_uat": "yarn cb:base-sepolia && graph deploy  --node http://graph-indexer-prod.airstack.xyz/  protocol_transactional_testnet_uat -l v0.0.0",
    "cbd:protocol_transactional_mainnet_uat": "yarn cb:base && graph deploy  --node http://graph-indexer-prod.airstack.xyz/  protocol_transactional_mainnet_uat -l v0.0.0",
    "cbd": "yarn cbd:protocol_transactional_testnet_uat && yarn cbd:protocol_transactional_mainnet_uat"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.73.0",
    "@graphprotocol/graph-ts": "0.35.1",
    "mustache": "^4.2.0"
  },
  "devDependencies": {
    "matchstick-as": "0.6.0"
  }
}
